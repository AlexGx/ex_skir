# Skir for Elixir

WIP

## Installation

If [available in Hex](https://hex.pm/docs/publish), the package can be installed
by adding `skir` to your list of dependencies in `mix.exs`:

```elixir
def deps do
  [
    {:ex_skir, "~> 0.1.0"}
  ]
end
```
